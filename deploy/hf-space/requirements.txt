# Runtime deps for the TruthLens backend on Hugging Face Spaces.
# Training-only deps (lime, matplotlib, seaborn, pytest) are intentionally omitted
# to keep the image small and the cold start fast.

# Core ML
numpy>=1.24.0
scikit-learn>=1.3.0
torch>=2.0.0
transformers>=4.35.0
joblib>=1.3.0

# Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pandas>=2.0.0
